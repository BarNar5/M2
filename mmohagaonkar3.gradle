defaultTasks 'jar'

apply plugin: 'java'

dependencies {
	compile files('lib/reources.jar')
}

jar { 
	manifest {
		attributes 'Implementation-Title' : 'M2', 'Class-path': '../../lib/resources.jar'
	}
}